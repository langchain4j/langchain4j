<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.thingsboard.langchain4j</groupId>
        <artifactId>langchain4j-parent</artifactId>
        <version>1.11.0-beta19-TB3</version>
        <relativePath>../../langchain4j-parent/pom.xml</relativePath>
    </parent>

    <artifactId>langchain4j-embeddings-e5-small-v2-q</artifactId>
    <name>LangChain4j :: Embeddings :: e5-small-v2 (quantized)</name>
    <description>In-process e5-small-v2 (quantized) embedding model</description>

    <properties>
        <embeddingsSkipCache>false</embeddingsSkipCache>
    </properties>

    <dependencies>

        <dependency>
            <groupId>org.thingsboard.langchain4j</groupId>
            <artifactId>langchain4j-embeddings</artifactId>
            <version>${project.version}</version>
        </dependency>

    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>com.googlecode.maven-download-plugin</groupId>
                <artifactId>download-maven-plugin</artifactId>
                <version>1.13.0</version>
                <executions>
                    <execution>
                        <id>download-model-file</id>
                        <goals>
                            <goal>wget</goal>
                        </goals>
                        <phase>generate-resources</phase>
                        <configuration>
                            <url>https://huggingface.co/Xenova/e5-small-v2/resolve/main/onnx/model_quantized.onnx</url>
                            <outputDirectory>${project.build.directory}/classes</outputDirectory>
                            <outputFileName>e5-small-v2-q.onnx</outputFileName>
                            <sha256>7d9092cb25f2bd1c023b7e8d2aa459044a02030ac880e5a59fdaf27af69f1ded</sha256>
                            <skipCache>${embeddingsSkipCache}</skipCache>
                            <readTimeOut>600000</readTimeOut> <!-- 10 minutes -->
                            <retries>5</retries>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

</project>
