/**
 * Translates LangChain4j embedding-store values and VecDB configuration into {@code DBMS_VECTOR_DATABASE} JSON, and
 * maps VecDB JSON responses back into Java values.
 *
 * <p>This package does not execute JDBC calls or decide table and index lifecycles.
 */
package dev.langchain4j.store.embedding.oracle.vecdb.mapper;
